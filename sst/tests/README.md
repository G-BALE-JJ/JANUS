# JANUS SST Tests

## 目标

本目录未来放 JANUS-local SST 测试。它不复用或复制参考仓库的测试目录。

## 当前状态

当前不放真实测试，不运行验证。

## 未来测试分层

- `small/`：最小硬件路径测试。
- 后续可增加 `integration/`：跨 P-Golem/D-Golem 的集成测试。
- 后续可增加 `stats/`：JANUS-specific stats 后处理。

